Directory holds examples of yaml files

- global-tag.yaml: declaration of a global tag with comments, and a map
- mapping.yaml: holds a map with 2 nodes holding different values.
- mappingother.yaml: same as mapping.yaml, but using a different format in file
- strings.yaml: holds a map with 3 nodes holding differently configured strings.